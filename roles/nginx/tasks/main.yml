---
- name: Install Nginx
  ansible.builtin.package:
    name: nginx
    state: present
  when: enable_nginx_lb

- name: Start and enable Nginx
  ansible.builtin.service:
    name: nginx
    state: started
    enabled: yes
  when: enable_nginx_lb

